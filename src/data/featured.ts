/**
 * Curated slugs for Featured Treatments (post-audit P1 fix): previously this
 * section rendered the exact same 7 treatments, same order, as the Explorer
 * grid directly above it. A short, deliberately-picked set — one per
 * register (a facial, a laser protocol, a regenerative therapy) — is what
 * actually earns a second, editorial pass instead of repeating the catalog.
 */
export const featuredTreatmentSlugs: string[] = [
  "hydraglow-facial",
  "luminous-pigmentation-correction",
  "renewal-rejuvenation-therapy",
];
